query getHabitDetails($nameSlug: String!) {
  getHabitDetails(nameSlug: $nameSlug) {
    habit {
      name
      dateFrom
      dateTo
      progress
      description
      isDone
      isCompleted
    }
    error
  }
}
